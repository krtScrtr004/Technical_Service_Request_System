@model TECHNICAL_SERVICE_REQUEST.Models.RequestDetailsViewModel

@{
    var id = Model.Id;
    var description = !string.IsNullOrEmpty(Model.Description)
        ? Model.Description.Trim()
        : "";
}


<section id="edit_description_modal" class="modal fade" role="dialog">
    <section class="modal-dialog">

        @* Modal content *@
        <section class="modal-content">

            @* Header *@
            <section class="modal-header bg-primary">
                <button type="button" class="close" data-dismiss="modal">&times;</button>

                <h4 class="modal-title text-light">
                    <i class="glyphicon glyphicon-pencil text-light"></i>
                    Edit Request Description
                </h4>
            </section>

            @using (Html.BeginForm(
                "EditDescription",
                "Request",
                new { id = id },
                FormMethod.Post
            ))
            {
                @Html.AntiForgeryToken();

                @* Body *@
                <section class="modal-body">
                    @Html.HiddenFor(model => model.Id)

                    <section class="form-group">
                        <label class="text-primary light-text" for="Description">Description:</label>
                        <textarea class="form-control" id="Description" name="Description" rows="10" min="0" maxlength="500" required>@description</textarea>
                    </section>

                    <p>
                        <em class="light-text primary-text">You may modify your request's description here</em>
                    </p>
                </section>

                @* Footer *@
                <section class="modal-footer">
                    <button type="submit" class="btn btn-primary ">
                        <i class="glyphicon glyphicon-floppy-save"></i>
                        Save
                    </button>
                </section>
            }

        </section>

    </section>
</section>

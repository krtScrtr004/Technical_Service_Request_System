# Copilot Instructions

## Project Guidelines
- Use custom utility classes together with Bootstrap 3 classes for responsive UI updates.
- For the TSR form page, keep overflow behavior at the `tsr-form` within `tsr-form-page-container` instead of making each table section scroll independently, to avoid print layout issues.